import { PawnStateWaitingPlacement } from './pawn-state-waiting-placement';

describe('PawnStateWaitingPlacement', () => {
  it('should create an instance', () => {
    expect(new PawnStateWaitingPlacement()).toBeTruthy();
  });
});
